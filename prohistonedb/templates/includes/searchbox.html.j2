<div class="container col-lg-8 col-sm-12 mx-auto mt-4">
    <div class="position-relative border-radius-xl bg-white shadow mb-3 px-4 gx-5 py-4">
        
        {% include 'includes/search_form.html.j2' %}

        <div class="row mt-3 d-none d-xl-block">
          {# TODO: remove examples when selecting dropdown option (or show only relevant example) #}
          <div class="col-md-12">
            <small>Examples:</small>
            {# TODO: use actual IDs from the database #}
            <span class="badge bg-light text-dark">UP000027093</span>{# example protein id #}
            <span class="badge bg-light text-dark">KCZ72620.1</span>{# example gen id #}
            <span class="badge bg-light text-dark"><a href="{{ url_for('main.entry', uniprot_id = 'A0A7X6DRI5') }}">A0A7X6DRI5</a></span>{# example Uniprot id #}
            <span class="badge bg-light text-dark">Candidatus Diapherotrites archaeon</span>{# example organism #}
          </div>
        </div>
    </div>
  </div>