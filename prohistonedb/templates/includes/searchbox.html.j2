<div class="container col-lg-8 col-sm-12 mx-auto mt-4 px-0">
    <div class="position-relative border-radius-xl bg-white shadow mb-3 px-4 gx-5 py-4">
        
        {% include 'includes/search_form.html.j2' %}

        <div class="row mt-3 d-none d-xl-block">
          {# TODO: remove examples when selecting dropdown option (or show only relevant example) #}
          <div class="col-md-12">
            <small>Examples:</small>
            <span class="badge bg-light text-dark"><a href="{{ url_for('search.index', pmid = 'UP000027093') }}">UP000027093</a></span>{# example protein id #}
            <span class="badge bg-light text-dark"><a href="{{ url_for('search.index', gen = 'ENI51_0006') }}">ENI51_00065</a></span>{# example gen id #}
            <span class="badge bg-light text-dark"><a href="{{ url_for('main.entry', uniprot_id = 'A0A831SMI2') }}">A0A831SMI2</a></span>{# example Uniprot id #}
            <span class="badge bg-light text-dark"><a href="{{ url_for('search.index', org = 'Candidatus Diapherotrites archaeon') }}">Candidatus Diapherotrites archaeon</a></span>{# example organism #}
          </div>
        </div>
    </div>
  </div>